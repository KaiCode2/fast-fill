# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (real secrets) — but DO commit the example
.env
.env.local
.env.development.local
.env.production.local
!.env.example

# generated ABIs are committed so the demo builds without Foundry (e.g. on Vercel);
# `pnpm gen:abis` regenerates them from ../out when forge artifacts are present.
